# Draft a reply

Write a short, calm reply to the customer below. Apply the policy exactly; do not
promise anything the policy does not cover.

Customer message:
{message}

Policy:
{assets[policy]}

Return JSON with `message` (the original customer message) and `reply` (your reply text).
